# Example environment variables
# URL de base de l'API utilisée par le frontend (remplacée au build)
VITE_API_BASE=https://api.example.com/api

# Origines autorisées pour les requêtes CORS (séparées par des virgules)
CORS_ORIGIN=https://benjamin15.github.io,https://benjamin15.github.io/art-gallery-portfoli

# Port du serveur Express
PORT=8787

# MongoDB (laisser vide pour fallback fichier KV)
MONGODB_URI=mongodb://localhost:27017
MONGODB_DB=artgallery

# Domaine public (pour reverse proxy Caddy / Nginx)
API_DOMAIN=api.example.com
# Email pour certificat Let's Encrypt (Caddy)
CADDY_EMAIL=you@example.com
